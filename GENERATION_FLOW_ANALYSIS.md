# 学習カード生成フローとキャパシティ分析

## 現在の生成フロー

### ステップ1: 単元基本情報生成（1回のAPI呼び出し）
```
POST /api/ai/generate-unit
├─ 入力: 学年、教科、教科書、単元名
├─ 出力: 単元目標、非認知目標、総時数
└─ 所要時間: 約5秒
```

### ステップ2: 3コース生成（3回のAPI呼び出し）
```
POST /api/ai/generate-course × 3回
├─ コース1: ゆっくりコース（6枚のカード）
├─ コース2: しっかりコース（6枚のカード）
└─ コース3: ぐんぐんコース（6枚のカード）
└─ 所要時間: 約30-45秒（各コース10-15秒）
```

### ステップ3: 選択問題生成（1回のAPI呼び出し）
```
POST /api/curriculum/:curriculumId/generate-course-problems
├─ コース選択問題（3問）
└─ 導入問題（3問）
└─ 所要時間: 約5-10秒
```

### **現在の合計生成時間: 約40-60秒**
- 単元情報: 5秒
- 3コース（18枚カード）: 30-45秒
- 選択問題: 5-10秒

---

## 検索練習コンテンツの追加提案

### 🎯 要望の整理

**ユーザー要望:**
1. 学習カード生成時に検索練習コンテンツも同時生成
2. 「学習のてびき」（カード詳細画面）に表示
3. 生成タイミング: カード生成時 or 選択問題生成時

**生成内容:**
- ケース10: よく出る問題（頻出パターン3-5問）
- ケース11: 応用問題（思考問題3-5問）
- ケース12: 総復習チェックリスト（4-6項目）

---

## 提案A: 選択問題生成時に追加（推奨）

### 理由:
1. **既に問題生成のフローがある**（`generate-course-problems`）
2. **生成内容が類似**（問題形式のコンテンツ）
3. **追加時間が最小**（+5-10秒程度）

### 実装:

```typescript
// 既存APIを拡張
app.post('/api/curriculum/:curriculumId/generate-course-problems', async (c) => {
  // ... 既存のコード ...
  
  const prompt = `小学${curriculum.grade}年 ${curriculum.subject}「${curriculum.unit_name}」の問題を生成。

【必須：3つのコース】
1. ${courses.results[0]?.course_name || 'ゆっくりコース'}
2. ${courses.results[1]?.course_name || 'しっかりコース'}  
3. ${courses.results[2]?.course_name || 'ぐんぐんコース'}

【新規追加：検索練習コンテンツ】
4. よく出る問題（頻出パターン5問）
5. 応用問題（思考力問題5問）
6. 総復習チェックリスト（6項目）

【必須：JSONのみ出力】
{
  "course_selection_problems": [...],
  "introduction_problems": [...],
  
  // 新規追加
  "retrieval_practice": {
    "frequent_problems": [
      {
        "problem_number": 1,
        "problem_title": "基本パターン1",
        "problem_content": "問題文（具体的な数字を含む）",
        "answer": "正解",
        "explanation": "解説（なぜこの答えになるか）",
        "time_limit": 10,
        "difficulty": "easy"
      },
      // ... 5問
    ],
    "application_problems": [
      {
        "problem_number": 1,
        "problem_title": "応用問題1",
        "problem_content": "問題文（複数の概念を組み合わせる）",
        "answer": "正解",
        "explanation": "解説（思考プロセスを含む）",
        "thinking_points": ["ポイント1", "ポイント2"],
        "difficulty": "medium"
      },
      // ... 5問
    ],
    "review_checklist": [
      {
        "item_number": 1,
        "check_point": "この単元で学んだこと1",
        "description": "具体的な確認内容",
        "example": "例: 3×4=12の意味を説明できる"
      },
      // ... 6項目
    ]
  }
}
`
  
  // ... API呼び出し ...
  
  // データベース保存（新規テーブル）
  await env.DB.prepare(`
    INSERT INTO retrieval_practice_content 
    (curriculum_id, content_type, problem_data)
    VALUES (?, ?, ?)
  `).bind(
    curriculumId,
    'frequent_problems',
    JSON.stringify(data.retrieval_practice.frequent_problems)
  ).run()
  
  // ... 同様にapplication_problems, review_checklistも保存 ...
})
```

### 新規テーブル設計:

```sql
-- 検索練習コンテンツテーブル
CREATE TABLE retrieval_practice_content (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  curriculum_id INTEGER NOT NULL,
  
  -- コンテンツタイプ
  content_type TEXT NOT NULL,  -- 'frequent_problems', 'application_problems', 'review_checklist'
  
  -- コンテンツデータ（JSON）
  problem_data TEXT NOT NULL,
  
  -- メタデータ
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  
  FOREIGN KEY (curriculum_id) REFERENCES curriculum(id),
  INDEX idx_curriculum_type (curriculum_id, content_type)
);
```

### 追加時間:
- **+5-10秒**（問題生成と同じAPI呼び出し内）
- **合計生成時間: 45-70秒**（現在の40-60秒から+5-10秒）

---

## 提案B: カード生成時に追加（より詳細）

### 理由:
1. **各コースに最適化されたコンテンツ**
2. **18枚のカード × 3タイプ = 54個のコンテンツ**
3. **より個別最適化**

### 実装:

```typescript
app.post('/api/ai/generate-course', async (c) => {
  // ... 既存のコード（6枚のカード生成）...
  
  // 各カードに検索練習コンテンツを追加
  const promptWithRetrieval = `${existingPrompt}

【追加：このカードの検索練習コンテンツ】
- よく出る問題（このカードの重要ポイント1問）
- 応用問題（このカードの発展的問題1問）
- 復習チェック（このカードの理解確認1項目）
`
  
  // ... 各カードに検索練習データを含めて生成 ...
})
```

### 追加時間:
- **+10-15秒**（各コースの生成時間が少し増加）
- **合計生成時間: 50-75秒**（現在の40-60秒から+10-15秒）

---

## 提案C: 後から生成（柔軟性重視）

### 理由:
1. **生成時間の増加を最小化**
2. **必要に応じて生成**
3. **教師が内容を確認してから追加生成**

### 実装:

```typescript
// 新規エンドポイント
app.post('/api/curriculum/:curriculumId/generate-retrieval-practice', async (c) => {
  // カリキュラム全体を対象に検索練習コンテンツを生成
})
```

### 追加時間:
- **初回生成時: 0秒**（通常通り）
- **追加生成時: +10-15秒**（ボタンクリックで生成）

---

## キャパシティ分析

### Gemini API制限（無料枠）:
- **RPM（Requests Per Minute）**: 15リクエスト/分
- **TPM（Tokens Per Minute）**: 1,000,000トークン/分
- **RPD（Requests Per Day）**: 1,500リクエスト/日

### 現在の使用量（1単元生成）:
- リクエスト数: 5回（単元1 + コース3 + 問題1）
- トークン数: 約50,000-70,000トークン
- 所要時間: 40-60秒

### 提案Aを採用した場合:
- リクエスト数: **5回**（変更なし、問題生成のプロンプトが拡張）
- トークン数: 約60,000-85,000トークン（+10,000-15,000トークン）
- 所要時間: 45-70秒（+5-10秒）

### 提案Bを採用した場合:
- リクエスト数: **5回**（変更なし、各コースのプロンプトが拡張）
- トークン数: 約65,000-95,000トークン（+15,000-25,000トークン）
- 所要時間: 50-75秒（+10-15秒）

### 提案Cを採用した場合:
- リクエスト数: **6回**（追加生成ボタンクリック時に+1）
- トークン数: 約60,000-85,000トークン（追加生成時）
- 所要時間: 初回40-60秒、追加生成10-15秒

---

## 結論と推奨案

### 🎯 推奨：提案A（選択問題生成時に追加）

**理由:**
1. ✅ **最小の追加時間**（+5-10秒）
2. ✅ **リクエスト数増加なし**（API制限に優しい）
3. ✅ **単元全体の検索練習コンテンツ**（統一的）
4. ✅ **実装が簡単**（既存エンドポイントの拡張）
5. ✅ **ユーザー要望に合致**（学習カード生成時に自動生成）

**デメリット:**
- 各カードに個別最適化されない（単元全体で共通）

### 実装ステップ（推奨案）:

```
Phase 1: デモの完成（今日）
├─ [x] 理論マッピング文書化
├─ [ ] ケース1-3のモーダル化
├─ [ ] 全12ケースのUI統一
└─ [ ] トップページの構成整理

Phase 2: API統合とデータ生成（次回）
├─ [ ] 新規テーブル作成（retrieval_practice_content）
├─ [ ] generate-course-problems APIの拡張
│    └─ プロンプトに検索練習コンテンツを追加
├─ [ ] 検索練習コンテンツのDB保存
└─ [ ] カード詳細画面に表示（学習のてびき）

Phase 3: 学習履歴と連携（将来）
├─ [ ] 検索練習APIとの連携
├─ [ ] 学習履歴の記録
├─ [ ] 効果測定ダッシュボード
└─ [ ] 教師による編集機能
```

---

## 生成内容の例（3×4の単元）

### よく出る問題（5問）:
1. **基本の式**: 3×4の答えは？（時間制限: 10秒）
2. **絵を見て式**: りんごが3個ずつ4箱あります。全部で何個？
3. **式を立てる**: 1袋に3個入ったキャンディが4袋あります。式は？
4. **答えから式**: 答えが12になるかけ算の式を書きましょう
5. **文章題**: 1本3円の鉛筆を4本買いました。代金は？

### 応用問題（5問）:
1. **逆算**: □×4=12 □にあてはまる数は？
2. **組み合わせ**: 3×4と4×3の違いを説明しましょう
3. **複合問題**: 3×4+2の答えは？（順序に注意）
4. **文章作成**: 3×4=12を使った文章題を作りましょう
5. **生活応用**: 3人に4個ずつお菓子を配ります。7個足りません。お菓子は全部で何個？

### 総復習チェックリスト（6項目）:
1. ☐ かけ算の意味（同じ数をたす）を理解している
2. ☐ 3×4の式を立てることができる
3. ☐ 3×4=12を計算できる
4. ☐ 絵や図を見て式を作れる
5. ☐ 文章題から式を立てられる
6. ☐ かけ算を使った問題を自分で作れる

---

## 表示場所: 学習のてびき

### 現在の構成:
```
学習カード詳細画面
├─ カード本文
├─ ヒント1-3
├─ 答え
├─ 実社会とのつながり
└─ 選択問題
```

### 新構成（提案）:
```
学習カード詳細画面
├─ カード本文
├─ ヒント1-3
├─ 答え
├─ 実社会とのつながり
├─ 選択問題
└─ 📝 学習のてびき（新規追加）← ここに表示
    ├─ 💡 よく出る問題
    ├─ 🧩 応用問題
    └─ ✅ 総復習チェックリスト
```

または

```
学習カード一覧画面（コースビュー）
├─ カード1-6
└─ 📚 単元の学習サポート（新規追加）← ここに表示
    ├─ 💡 よく出る問題（ケース10）
    ├─ 🧩 応用問題（ケース11）
    └─ ✅ 総復習（ケース12）
```

---

## 質問への回答

### Q1: 生成時間などのキャパシティーで可能かどうか？
**A: 可能です。推奨案（提案A）で+5-10秒の追加時間。**

| 項目 | 現在 | 提案A | 増加 |
|------|------|-------|------|
| 生成時間 | 40-60秒 | 45-70秒 | +5-10秒 |
| APIリクエスト | 5回 | 5回 | 変更なし |
| トークン使用量 | 50k-70k | 60k-85k | +10k-15k |
| RPM制限への影響 | 問題なし | 問題なし | 変更なし |

### Q2: 生成も学習カード等の生成を行う1回目か2回目の選択問題などを生成に入れるべきか？
**A: 2回目（選択問題生成時）に追加することを推奨します。**

**理由:**
1. 既に問題生成のフローがある
2. 追加時間が最小（+5-10秒）
3. リクエスト数が増えない
4. 単元全体の統一的なコンテンツ

**ただし、将来的にはPhase 3で各カードに個別最適化されたコンテンツを追加することも可能です。**
